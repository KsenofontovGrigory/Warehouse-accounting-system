import { styled } from '@mui/material';

export const ContainerModal = styled('div')({
    display: 'flex',
    justifyContent: 'space-between',
    alignItems: 'center',
    fontSize: '28px',

    'input': {
        color: 'white'
    },
})