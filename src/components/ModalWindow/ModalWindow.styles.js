import {styled} from '@mui/material';

export const Title = styled('div')({
    fontSize: '40px',
})

export const TextError = styled('p')({
    fontSize: '32px',
    textAlign: 'center',
    color: 'red',
})
